# دليل التشغيل على GitHub + Netlify (5 دقائق)

هذا الدليل يجعل نظام «دفتر» يعمل على Netlify بحيث **أي تعديل يقوم به أي شخص يظهر لكل من يملك الرابط**.

> **لماذا نحتاج هذه الخطوات؟**
> GitHub وNetlify يستضيفان الملفات فقط (HTML/CSS/JS) وليس فيهما قاعدة بيانات.
> لذلك نربط النظام بقاعدة بيانات سحابية **مجانية** من Google (Firebase Realtime Database)
> تعمل مباشرة من المتصفح دون أي خادم. كل ما تحتاجه: لصق رابط واحد في ملف واحد.

---

## الخطوة 1 — إنشاء قاعدة البيانات (مرة واحدة فقط)

1. افتح <https://console.firebase.google.com> وسجّل الدخول بحساب Google.
2. اضغط **Add project / إنشاء مشروع** → اكتب اسمًا (مثل `daftar`) → أوقف Google Analytics (غير مطلوب) → **Create project**.
3. من القائمة الجانبية: **Build ← Realtime Database** → **Create Database**.
4. اختر أي موقع (Location) → التالي → اختر **Start in test mode** → **Enable**.
5. ستظهر لك صفحة القاعدة وفي أعلاها **رابط** بشكل:
   - `https://daftar-xxxxx-default-rtdb.firebaseio.com`
   - أو `https://daftar-xxxxx-default-rtdb.europe-west1.firebasedatabase.app`

   **انسخ هذا الرابط.**

6. افتح تبويب **Rules** داخل القاعدة والصق القواعد التالية ثم **Publish**
   (وضع الاختبار ينتهي بعد 30 يومًا، هذه القواعد تجعله دائمًا):

```json
{
  "rules": {
    "daftar": {
      ".read": true,
      ".write": true
    }
  }
}
```

> ⚠️ هذه القواعد تعني أن **كل من يعرف الرابط يستطيع القراءة والتعديل** — وهذا هو المطلوب في نظامك (لا كلمة مرور). لا تنشر الرابط علنًا؛ شاركه فقط مع فريقك.

---

## الخطوة 2 — ضع الرابط في ملف الإعدادات

افتح الملف **`js/config.js`** بأي محرر نصوص (حتى Notepad) وستجد السطر:

```js
firebaseDatabaseUrl: '',
```

الصق الرابط بين علامتي التنصيص:

```js
firebaseDatabaseUrl: 'https://daftar-xxxxx-default-rtdb.firebaseio.com',
```

احفظ الملف. **هذا هو التعديل الوحيد المطلوب.**

---

## الخطوة 3 — الرفع على GitHub

1. افتح مستودعك على GitHub → **Add file ← Upload files**.
2. اسحب **كل** الملفات والمجلدات من داخل مجلد `daftar-project` (بعد فك الضغط):
   `index.html`, `download.html`, `netlify.toml`, `.nojekyll`, `README.md`, `SETUP.md`, ومجلدي `css/` و`js/` كاملين.
3. **Commit changes**.

> يمكنك أيضًا تعديل `js/config.js` مباشرة من GitHub (زر القلم ✏️) إن نسيت الخطوة 2.

---

## الخطوة 4 — Netlify

- إذا كان موقعك مربوطًا بالمستودع فسينشر تلقائيًا خلال دقيقة.
- وإلا: Netlify → **Add new site ← Import an existing project ← GitHub** → اختر المستودع → اترك إعدادات البناء فارغة (Publish directory: `.`) → **Deploy**.

افتح الرابط. يجب أن ترى في أسفل الصفحة: **«متصل بـ Firebase · البيانات مشتركة ومحدثة»**.

---

## الخطوة 5 — نقل بياناتك القديمة (اختياري)

1. افتح نسختك القديمة (المحلية) → **لوحة التحكم ← تصدير نسخة كاملة** → يُحمَّل ملف JSON.
2. افتح النسخة الجديدة → **لوحة التحكم ← استيراد نسخة** → اختر الملف.
3. تصبح كل العمليات والديون والتصنيفات على السحابة ومتاحة لجميع الأجهزة.

---

## التحقق من أن كل شيء يعمل

- افتح الرابط من جهازين (أو المتصفح + الجوال) → أضف عملية من أحدهما → تظهر على الآخر خلال ثوانٍ (المزامنة كل 15 ثانية أو عند العودة للتبويب).

## مشاكل شائعة

| الرسالة | الحل |
|---|---|
| «هذه الاستضافة لا توفر خادم بيانات…» | لم تضع رابط Firebase في `js/config.js` أو لم ترفع الملف المعدّل. |
| «قاعدة البيانات ترفض الوصول» | القواعد (Rules) غير منشورة أو انتهى وضع الاختبار. طبّق الخطوة 1-6. |
| «لا يوجد اتصال بالخادم» | تحقق من الإنترنت أو من صحة الرابط (يجب أن يبدأ بـ https://). |
| التعديلات لا تظهر بعد الرفع | انتظر انتهاء نشر Netlify ثم حدّث الصفحة بـ Ctrl+F5. |

## الحد المجاني في Firebase
1 GB تخزين و10 GB نقل شهريًا — أكثر بكثير من احتياج دفتر محاسبة صغير.
**نصيحة:** الصور تُخزَّن داخل السجلات؛ استخدم صورًا صغيرة (أقل من 1 MB) لتبقى القاعدة سريعة.
