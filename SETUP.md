# دليل النشر على GitHub + Netlify (بدون أي إعداد)

يعمل نظام «دفتر» بنفس الآلية التقنية لمتجر CampusKart: **Netlify Functions + Netlify Blobs**.
البيانات تُحفظ على خادم Netlify نفسه، لذلك **أي تعديل من أي جهاز يظهر لكل من يملك الرابط**.

لا تحتاج قاعدة بيانات خارجية، ولا متغيرات بيئة، ولا أي تعديل في الملفات.

---

## الخطوة 1 — الرفع على GitHub

1. فك ضغط `daftar-project.zip`.
2. افتح مستودعك على GitHub → **Add file ← Upload files**.
3. اسحب **كل** ما داخل مجلد `daftar-project`:

```
index.html
download.html
netlify.toml        ← ضروري
package.json        ← ضروري
README.md, SETUP.md, START-HERE.txt
css/                ← المجلد كاملًا
js/                 ← المجلد كاملًا
netlify/functions/  ← المجلد كاملًا (ledger.js, photo.js)
```

4. **Commit changes**.

> ⚠️ لا ترفع `index.html` وحده. المجلدات `css` و`js` و`netlify` كلها ضرورية.
> إذا كان المستودع يحوي النسخة القديمة، احذف ملفاتها أولًا أو ارفع فوقها (استبدال).

---

## الخطوة 2 — Netlify

- **إذا كان الموقع مربوطًا بالمستودع مسبقًا:** لا تفعل شيئًا؛ ينشر تلقائيًا خلال دقيقة أو دقيقتين.
- **إذا كان موقعًا جديدًا:** Netlify → **Add new site ← Import an existing project ← GitHub** → اختر المستودع → اترك الإعدادات كما هي (يقرأها من `netlify.toml`) → **Deploy**.

---

## الخطوة 3 — افتح الرابط

يجب أن ترى في أسفل الصفحة:
**«متصل · البيانات مشتركة ومحدثة لكل من يملك الرابط»**

## كيف تُحفظ التعديلات؟ (زر «رفع التعديلات»)

- أي إضافة أو تعديل أو حذف يُحفظ **على جهازك أولًا** ويظهر لك فورًا.
- يظهر شريط برتقالي أعلى الصفحة بعدد التغييرات المعلّقة وزرّ **«رفع التعديلات»**.
- أجرِ ما تريد من تعديلات، ثم اضغط الزر مرة واحدة → تُرسل كلها للخادم ويراها كل من يملك الرابط خلال 15 ثانية.
- **«إلغاء التعديلات»** يُرجعك إلى النسخة المحفوظة على الخادم.
- التعديلات المعلّقة لا تُفقد عند تحديث الصفحة، ويحذّرك المتصفح إن حاولت إغلاق التبويب قبل رفعها.

جرّب: افتح الرابط من الجوال والحاسوب → أضف عملية من أحدهما واضغط «رفع التعديلات» → تظهر على الآخر خلال 15 ثانية.

---

## نقل بياناتك القديمة (اختياري)

1. افتح النسخة القديمة → **لوحة التحكم ← تصدير نسخة كاملة** → يُحمَّل ملف JSON.
2. افتح النسخة الجديدة → **لوحة التحكم ← استيراد نسخة** → اختر الملف.

---

## كيف يعمل؟ (للاطلاع فقط)

| الجزء | الوظيفة |
|---|---|
| `netlify/functions/ledger.js` | `/api/ledger` — قراءة وحفظ العمليات والديون والإعدادات والسجل في Netlify Blobs |
| `netlify/functions/photo.js` | `/api/photo` — رفع صور الفواتير وعرضها |
| `js/storage.js` | يتصل بالدوال أعلاه ويتحقق كل 15 ثانية من تعديلات الأجهزة الأخرى |

Netlify Blobs مجاني ضمن الخطة المجانية (ويكفي بكثير لدفتر محاسبة).

---

## مشاكل شائعة

| الرسالة / العرض | السبب والحل |
|---|---|
| «لم يُعثر على خادم البيانات…» | لم يُرفع مجلد `netlify` أو ملف `netlify.toml` أو `package.json`. تأكد من وجودها في جذر المستودع ثم أعد النشر. |
| الموقع يعمل على GitHub Pages لكن لا يحفظ | طبيعي؛ GitHub Pages لا يشغّل الدوال. استخدم رابط Netlify فقط. |
| «تعذّر حفظ البيانات» | افتح Netlify → Deploys → Functions وتأكد أن `ledger` و`photo` ظاهرتان. إن لم تظهرا، تحقق من رفع `package.json`. |
| التعديلات لا تظهر بعد الرفع | انتظر انتهاء النشر (Deploys ← Published) ثم حدّث الصفحة بـ Ctrl+F5. |
| صورة لا تظهر | الصور القديمة (base64 من النسخة المحلية) تُعرض كما هي؛ الصور الجديدة تُرفع للخادم. تأكد من الاتصال. |

## الأمان
لا توجد كلمة مرور — كل من يملك الرابط يستطيع القراءة والتعديل (حسب طلبك). الموقع مضبوط على `noindex` حتى لا يظهر في محركات البحث، لكن شارك الرابط مع من تثق به فقط.
