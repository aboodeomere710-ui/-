# رفع دفتر على GitHub Pages

## التحميل
افتح download.html وانتظر تجهيز ZIP، ثم اضغط رابط «تحميل الآن — daftar-project.zip» بنفسك. تجهيز الملف لا يعني حفظه تلقائيًا. إذا منع المتصفح التنزيل داخل المعاينة، افتح صفحة التنزيل في تبويب مستقل أو متصفح عادي.

## فك الضغط والرفع
1. فك ضغط daftar-project.zip وافتح مجلد daftar-project.
2. أنشئ مستودع GitHub جديدًا للمحاسبة حتى لا تستبدل موقع المتجر القديم دون قصد.
3. اختر Add file → Upload files وارفع **محتويات** مجلد daftar-project إلى جذر المستودع، وليس ملف ZIP ولا المجلد الخارجي.
4. يجب أن يكون index.html في الجذر وبجانبه مجلدا css وjs بكامل محتوياتهما (بما فيها js/vendor)، وdownload.html وبقية ملفات المصدر والتعليمات.
5. الأرشيف يشمل .nojekyll وهو ملف فارغ قد يكون مخفيًا في مدير الملفات، لعرض الموقع الثابت دون معالجة Jekyll.
6. احفظ الملفات بـ Commit على الفرع main. لا توجد أوامر npm أو build أو مفاتيح API مطلوبة.

## تشغيل الرابط — مرة واحدة
من المستودع افتح Settings → Pages، ثم اختر:
- Source: Deploy from a branch
- Branch: main
- Folder: / (root)
ثم Save، وانتظر نجاح النشر. افتح الرابط الذي يظهر في GitHub Pages؛ رابط github.com للمستودع نفسه ليس رابط الموقع.

الصيغة المعتادة لرابط الموقع: https://USERNAME.github.io/REPOSITORY/
جميع مسارات المشروع نسبية وتعمل داخل مسار المستودع.

## التحديث والبيانات
ارفع التعديلات على نفس الفرع ثم انتظر نشر Pages. تظهر ملفات الواجهة الجديدة بعد إعادة تحميل الصفحة؛ النسخ المنزلة والتبويبات القديمة لا تتحدث تلقائيًا.
البيانات المالية والإعدادات الإدارية محلية لكل متصفح، ولا تتحول إلى بيانات مشتركة بمجرد نشر الموقع. لنقل البيانات صدّر JSON من الموقع القديم واستورده في الجديد. لا ترفع ملفات النسخ الاحتياطية أو بياناتك المالية إلى مستودع عام.

## حل المشاكل
- 404: تأكد من نجاح النشر ووجود index.html في الجذر.
- تصميم ناقص: ارفع مجلدي css وjs كاملين مع الحفاظ على الأسماء وحالة الأحرف.
- تنزيل محظور: افتح download.html في تبويب مستقل، انتظر ظهور الرابط الجاهز، ثم اضغطه. إذا فتح المتصفح معاينة الملف استخدم حفظ/مشاركة الملفات.
- البيانات غير موجودة: تغيير العنوان أو الجهاز لا ينقل IndexedDB؛ استخدم النسخة الاحتياطية.
- للتشغيل المفرد استخدم daftar-system.html في متصفح يدعم IndexedDB للملفات المحلية؛ دعم file:// يختلف بين المتصفحات.
